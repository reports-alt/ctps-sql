rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
      match /tables/{table} {
        allow read: if true;
      }
       match /inventory/{loc} {
        allow read: if true;
      }
    }
    
  }
}